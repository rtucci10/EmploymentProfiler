Modify EmploymentProfilerModel.tt
   -Create separate repository files from T4 generation
   -Create repository factory to allow other underlying ORM like nHibernate

Create MVC web application
Create thin communication layer between client and server = WCF